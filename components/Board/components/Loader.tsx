import * as React from 'react'

// custom imports
import { LoaderDiv, LoadingBar } from '../styles/Loader'

export default () => (
  <LoaderDiv>
    <LoadingBar />
    <LoadingBar />
    <LoadingBar />
    <LoadingBar />
  </LoaderDiv>
)
